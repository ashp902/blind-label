Custom OCR Model + Gemini/Qwen-VL Fine-Tuning Pipeline

This repository contains all training code, dataset preparation scripts, notebooks, annotation files, model checkpoints, and evaluation logs used to develop the OCR and AI components for the Accessible Food Label Reader mobile application.

This is Repo A, which includes only the machine learning and model training Notebooks.
